/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { addonsPostAddons } from "../../funcs/addonsPostAddons.js";
import { DtoCreateAddonRequest$zodSchema } from "../../models/dtocreateaddonrequest.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: DtoCreateAddonRequest$zodSchema,
};

export const tool$addonsPostAddons: ToolDefinition<typeof args> = {
  name: "addons-post-addons",
  description: `Create addon

Create a new addon`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await addonsPostAddons(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
