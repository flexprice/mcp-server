/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { environmentsGetEnvironments } from "../../funcs/environmentsGetEnvironments.js";
import { GetEnvironmentsRequest$zodSchema } from "../../models/getenvironmentsop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetEnvironmentsRequest$zodSchema.optional(),
};

export const tool$environmentsGetEnvironments: ToolDefinition<typeof args> = {
  name: "environments-get-environments",
  description: `Get environments

Get environments`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await environmentsGetEnvironments(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
