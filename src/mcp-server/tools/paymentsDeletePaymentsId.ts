/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { paymentsDeletePaymentsId } from "../../funcs/paymentsDeletePaymentsId.js";
import { DeletePaymentsIdRequest$zodSchema } from "../../models/deletepaymentsidop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: DeletePaymentsIdRequest$zodSchema,
};

export const tool$paymentsDeletePaymentsId: ToolDefinition<typeof args> = {
  name: "payments-delete-payments-id",
  description: `Delete a payment

Delete a payment`,
  annotations: {
    "title": "",
    "destructiveHint": true,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await paymentsDeletePaymentsId(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
