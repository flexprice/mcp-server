/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { walletsGetWalletsIdBalanceRealTime } from "../../funcs/walletsGetWalletsIdBalanceRealTime.js";
import { GetWalletsIdBalanceRealTimeRequest$zodSchema } from "../../models/getwalletsidbalancerealtimeop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetWalletsIdBalanceRealTimeRequest$zodSchema,
};

export const tool$walletsGetWalletsIdBalanceRealTime: ToolDefinition<
  typeof args
> = {
  name: "wallets-get-wallets-id-balance-real-time",
  description: `Get wallet balance

Get real-time balance of a wallet`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await walletsGetWalletsIdBalanceRealTime(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
