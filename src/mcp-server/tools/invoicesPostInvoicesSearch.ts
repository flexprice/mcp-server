/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { invoicesPostInvoicesSearch } from "../../funcs/invoicesPostInvoicesSearch.js";
import { TypesInvoiceFilter$zodSchema } from "../../models/typesinvoicefilter.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: TypesInvoiceFilter$zodSchema,
};

export const tool$invoicesPostInvoicesSearch: ToolDefinition<typeof args> = {
  name: "invoices-post-invoices-search",
  description: `List invoices by filter

List invoices by filter`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await invoicesPostInvoicesSearch(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
