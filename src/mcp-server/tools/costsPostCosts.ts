/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { costsPostCosts } from "../../funcs/costsPostCosts.js";
import { DtoCreateCostsheetRequest$zodSchema } from "../../models/dtocreatecostsheetrequest.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: DtoCreateCostsheetRequest$zodSchema,
};

export const tool$costsPostCosts: ToolDefinition<typeof args> = {
  name: "costs-post-costs",
  description: `Create a new costsheet

Create a new costsheet with the specified name`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await costsPostCosts(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
