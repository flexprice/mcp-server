/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { eventsGetEventsId } from "../../funcs/eventsGetEventsId.js";
import { GetEventsIdRequest$zodSchema } from "../../models/geteventsidop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetEventsIdRequest$zodSchema,
};

export const tool$eventsGetEventsId: ToolDefinition<typeof args> = {
  name: "events-get-events-id",
  description: `Get event by ID

Retrieve event details and processing status with debug information`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await eventsGetEventsId(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
