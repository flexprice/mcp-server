/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { subscriptionsGetSubscriptionsIdPauses } from "../../funcs/subscriptionsGetSubscriptionsIdPauses.js";
import { GetSubscriptionsIdPausesRequest$zodSchema } from "../../models/getsubscriptionsidpausesop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetSubscriptionsIdPausesRequest$zodSchema,
};

export const tool$subscriptionsGetSubscriptionsIdPauses: ToolDefinition<
  typeof args
> = {
  name: "subscriptions-get-subscriptions-id-pauses",
  description: `List all pauses for a subscription

List all pauses for a subscription`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await subscriptionsGetSubscriptionsIdPauses(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
