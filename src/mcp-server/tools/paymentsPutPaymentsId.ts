/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { paymentsPutPaymentsId } from "../../funcs/paymentsPutPaymentsId.js";
import { PutPaymentsIdRequest$zodSchema } from "../../models/putpaymentsidop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: PutPaymentsIdRequest$zodSchema,
};

export const tool$paymentsPutPaymentsId: ToolDefinition<typeof args> = {
  name: "payments-put-payments-id",
  description: `Update a payment

Update a payment with the specified configuration`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await paymentsPutPaymentsId(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
