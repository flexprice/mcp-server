/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { groupsDeleteGroupsId } from "../../funcs/groupsDeleteGroupsId.js";
import { DeleteGroupsIdRequest$zodSchema } from "../../models/deletegroupsidop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: DeleteGroupsIdRequest$zodSchema,
};

export const tool$groupsDeleteGroupsId: ToolDefinition<typeof args> = {
  name: "groups-delete-groups-id",
  description: `Delete a group

Delete a group and remove all entity associations`,
  annotations: {
    "title": "",
    "destructiveHint": true,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await groupsDeleteGroupsId(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
