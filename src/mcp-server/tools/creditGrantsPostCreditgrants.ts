/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { creditGrantsPostCreditgrants } from "../../funcs/creditGrantsPostCreditgrants.js";
import { DtoCreateCreditGrantRequest$zodSchema } from "../../models/dtocreatecreditgrantrequest.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: DtoCreateCreditGrantRequest$zodSchema,
};

export const tool$creditGrantsPostCreditgrants: ToolDefinition<typeof args> = {
  name: "credit-grants-post-creditgrants",
  description: `Create a new credit grant

Create a new credit grant with the specified configuration`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await creditGrantsPostCreditgrants(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
