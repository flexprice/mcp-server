/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { invoicesPostInvoicesIdFinalize } from "../../funcs/invoicesPostInvoicesIdFinalize.js";
import { PostInvoicesIdFinalizeRequest$zodSchema } from "../../models/postinvoicesidfinalizeop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: PostInvoicesIdFinalizeRequest$zodSchema,
};

export const tool$invoicesPostInvoicesIdFinalize: ToolDefinition<typeof args> =
  {
    name: "invoices-post-invoices-id-finalize",
    description: `Finalize an invoice

Finalize a draft invoice`,
    annotations: {
      "title": "",
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false,
      "readOnlyHint": false,
    },
    args,
    tool: async (client, args, ctx) => {
      const [result] = await invoicesPostInvoicesIdFinalize(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(result.value);
    },
  };
