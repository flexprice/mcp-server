/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { couponsPostCoupons } from "../../funcs/couponsPostCoupons.js";
import { DtoCreateCouponRequest$zodSchema } from "../../models/dtocreatecouponrequest.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: DtoCreateCouponRequest$zodSchema,
};

export const tool$couponsPostCoupons: ToolDefinition<typeof args> = {
  name: "coupons-post-coupons",
  description: `Create a new coupon

Creates a new coupon`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await couponsPostCoupons(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
