/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { costsPostCostsSearch } from "../../funcs/costsPostCostsSearch.js";
import { CostsheetFilter$zodSchema } from "../../models/costsheetfilter.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: CostsheetFilter$zodSchema,
};

export const tool$costsPostCostsSearch: ToolDefinition<typeof args> = {
  name: "costs-post-costs-search",
  description: `List costsheets by filter

List costsheet records by filter with POST body`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await costsPostCostsSearch(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
