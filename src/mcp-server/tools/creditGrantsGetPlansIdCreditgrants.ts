/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { creditGrantsGetPlansIdCreditgrants } from "../../funcs/creditGrantsGetPlansIdCreditgrants.js";
import { GetPlansIdCreditgrantsRequest$zodSchema } from "../../models/getplansidcreditgrantsop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetPlansIdCreditgrantsRequest$zodSchema,
};

export const tool$creditGrantsGetPlansIdCreditgrants: ToolDefinition<
  typeof args
> = {
  name: "credit-grants-get-plans-id-creditgrants",
  description: `Get plan credit grants

Get all credit grants for a plan`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await creditGrantsGetPlansIdCreditgrants(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
