/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { customersGetCustomersId } from "../../funcs/customersGetCustomersId.js";
import { GetCustomersIdRequest$zodSchema } from "../../models/getcustomersidop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetCustomersIdRequest$zodSchema,
};

export const tool$customersGetCustomersId: ToolDefinition<typeof args> = {
  name: "customers-get-customers-id",
  description: `Get a customer

Get a customer`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await customersGetCustomersId(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
