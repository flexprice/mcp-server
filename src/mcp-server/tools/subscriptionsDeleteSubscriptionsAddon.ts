/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { subscriptionsDeleteSubscriptionsAddon } from "../../funcs/subscriptionsDeleteSubscriptionsAddon.js";
import { DtoRemoveAddonRequest$zodSchema } from "../../models/dtoremoveaddonrequest.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: DtoRemoveAddonRequest$zodSchema,
};

export const tool$subscriptionsDeleteSubscriptionsAddon: ToolDefinition<
  typeof args
> = {
  name: "subscriptions-delete-subscriptions-addon",
  description: `Remove addon from subscription

Remove an addon from a subscription`,
  annotations: {
    "title": "",
    "destructiveHint": true,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await subscriptionsDeleteSubscriptionsAddon(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
