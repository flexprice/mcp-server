/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { entitlementsGetEntitlements } from "../../funcs/entitlementsGetEntitlements.js";
import { GetEntitlementsRequest$zodSchema } from "../../models/getentitlementsop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetEntitlementsRequest$zodSchema.optional(),
};

export const tool$entitlementsGetEntitlements: ToolDefinition<typeof args> = {
  name: "entitlements-get-entitlements",
  description: `Get entitlements

Get entitlements with the specified filter`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await entitlementsGetEntitlements(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
