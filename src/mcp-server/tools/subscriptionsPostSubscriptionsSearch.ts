/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { subscriptionsPostSubscriptionsSearch } from "../../funcs/subscriptionsPostSubscriptionsSearch.js";
import { TypesSubscriptionFilter$zodSchema } from "../../models/typessubscriptionfilter.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: TypesSubscriptionFilter$zodSchema,
};

export const tool$subscriptionsPostSubscriptionsSearch: ToolDefinition<
  typeof args
> = {
  name: "subscriptions-post-subscriptions-search",
  description: `List subscriptions by filter

List subscriptions by filter`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await subscriptionsPostSubscriptionsSearch(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
