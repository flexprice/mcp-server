/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { entitlementsGetEntitlementsId } from "../../funcs/entitlementsGetEntitlementsId.js";
import { GetEntitlementsIdRequest$zodSchema } from "../../models/getentitlementsidop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetEntitlementsIdRequest$zodSchema,
};

export const tool$entitlementsGetEntitlementsId: ToolDefinition<typeof args> = {
  name: "entitlements-get-entitlements-id",
  description: `Get an entitlement by ID

Get an entitlement by ID`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await entitlementsGetEntitlementsId(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
