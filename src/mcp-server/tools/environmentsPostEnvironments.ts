/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { environmentsPostEnvironments } from "../../funcs/environmentsPostEnvironments.js";
import { DtoCreateEnvironmentRequest$zodSchema } from "../../models/dtocreateenvironmentrequest.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: DtoCreateEnvironmentRequest$zodSchema,
};

export const tool$environmentsPostEnvironments: ToolDefinition<typeof args> = {
  name: "environments-post-environments",
  description: `Create an environment

Create an environment`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await environmentsPostEnvironments(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
