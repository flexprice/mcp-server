/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { featuresPutFeaturesId } from "../../funcs/featuresPutFeaturesId.js";
import { PutFeaturesIdRequest$zodSchema } from "../../models/putfeaturesidop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: PutFeaturesIdRequest$zodSchema,
};

export const tool$featuresPutFeaturesId: ToolDefinition<typeof args> = {
  name: "features-put-features-id",
  description: `Update a feature

Update a feature by ID`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await featuresPutFeaturesId(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
