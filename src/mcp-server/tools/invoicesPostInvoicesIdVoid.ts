/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { invoicesPostInvoicesIdVoid } from "../../funcs/invoicesPostInvoicesIdVoid.js";
import { PostInvoicesIdVoidRequest$zodSchema } from "../../models/postinvoicesidvoidop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: PostInvoicesIdVoidRequest$zodSchema,
};

export const tool$invoicesPostInvoicesIdVoid: ToolDefinition<typeof args> = {
  name: "invoices-post-invoices-id-void",
  description: `Void an invoice

Void an invoice that hasn't been paid`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await invoicesPostInvoicesIdVoid(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
