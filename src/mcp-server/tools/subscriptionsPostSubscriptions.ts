/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { subscriptionsPostSubscriptions } from "../../funcs/subscriptionsPostSubscriptions.js";
import { DtoCreateSubscriptionRequest$zodSchema } from "../../models/dtocreatesubscriptionrequest.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: DtoCreateSubscriptionRequest$zodSchema,
};

export const tool$subscriptionsPostSubscriptions: ToolDefinition<typeof args> =
  {
    name: "subscriptions-post-subscriptions",
    description: `Create subscription

Create a new subscription`,
    annotations: {
      "title": "",
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false,
      "readOnlyHint": false,
    },
    args,
    tool: async (client, args, ctx) => {
      const [result] = await subscriptionsPostSubscriptions(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(result.value);
    },
  };
