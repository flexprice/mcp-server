/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { tasksPutTasksIdStatus } from "../../funcs/tasksPutTasksIdStatus.js";
import { PutTasksIdStatusRequest$zodSchema } from "../../models/puttasksidstatusop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: PutTasksIdStatusRequest$zodSchema,
};

export const tool$tasksPutTasksIdStatus: ToolDefinition<typeof args> = {
  name: "tasks-put-tasks-id-status",
  description: `Update task status

Update a task's status`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await tasksPutTasksIdStatus(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
