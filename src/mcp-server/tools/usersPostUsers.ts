/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { usersPostUsers } from "../../funcs/usersPostUsers.js";
import { DtoCreateUserRequest$zodSchema } from "../../models/dtocreateuserrequest.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: DtoCreateUserRequest$zodSchema,
};

export const tool$usersPostUsers: ToolDefinition<typeof args> = {
  name: "users-post-users",
  description: `Create service account

Create a new service account with required roles. Only service accounts can be created via this endpoint.`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await usersPostUsers(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
