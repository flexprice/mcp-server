/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { tenantsGetTenantsId } from "../../funcs/tenantsGetTenantsId.js";
import { GetTenantsIdRequest$zodSchema } from "../../models/gettenantsidop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetTenantsIdRequest$zodSchema,
};

export const tool$tenantsGetTenantsId: ToolDefinition<typeof args> = {
  name: "tenants-get-tenants-id",
  description: `Get tenant by ID

Get tenant by ID`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await tenantsGetTenantsId(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
