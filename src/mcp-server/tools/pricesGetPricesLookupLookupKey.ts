/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { pricesGetPricesLookupLookupKey } from "../../funcs/pricesGetPricesLookupLookupKey.js";
import { GetPricesLookupLookupKeyRequest$zodSchema } from "../../models/getpriceslookuplookupkeyop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetPricesLookupLookupKeyRequest$zodSchema,
};

export const tool$pricesGetPricesLookupLookupKey: ToolDefinition<typeof args> =
  {
    name: "prices-get-prices-lookup-lookup-key",
    description: `Get price by lookup key

Get price by lookup key`,
    annotations: {
      "title": "",
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false,
      "readOnlyHint": true,
    },
    args,
    tool: async (client, args, ctx) => {
      const [result] = await pricesGetPricesLookupLookupKey(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(result.value);
    },
  };
