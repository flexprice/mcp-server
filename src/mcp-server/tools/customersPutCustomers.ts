/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { customersPutCustomers } from "../../funcs/customersPutCustomers.js";
import { PutCustomersRequest$zodSchema } from "../../models/putcustomersop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: PutCustomersRequest$zodSchema,
};

export const tool$customersPutCustomers: ToolDefinition<typeof args> = {
  name: "customers-put-customers",
  description: `Update a customer

Update a customer by id or external_customer_id`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await customersPutCustomers(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
