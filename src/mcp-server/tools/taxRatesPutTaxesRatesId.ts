/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { taxRatesPutTaxesRatesId } from "../../funcs/taxRatesPutTaxesRatesId.js";
import { PutTaxesRatesIdRequest$zodSchema } from "../../models/puttaxesratesidop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: PutTaxesRatesIdRequest$zodSchema,
};

export const tool$taxRatesPutTaxesRatesId: ToolDefinition<typeof args> = {
  name: "tax-rates-put-taxes-rates-id",
  description: `Update a tax rate

Update a tax rate`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await taxRatesPutTaxesRatesId(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
