/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { entitlementsPutEntitlementsId } from "../../funcs/entitlementsPutEntitlementsId.js";
import { PutEntitlementsIdRequest$zodSchema } from "../../models/putentitlementsidop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: PutEntitlementsIdRequest$zodSchema,
};

export const tool$entitlementsPutEntitlementsId: ToolDefinition<typeof args> = {
  name: "entitlements-put-entitlements-id",
  description: `Update an entitlement

Update an entitlement with the specified configuration`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await entitlementsPutEntitlementsId(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
