/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { customersPostCustomers } from "../../funcs/customersPostCustomers.js";
import { DtoCreateCustomerRequest$zodSchema } from "../../models/dtocreatecustomerrequest.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: DtoCreateCustomerRequest$zodSchema,
};

export const tool$customersPostCustomers: ToolDefinition<typeof args> = {
  name: "customers-post-customers",
  description: `Create a customer

Create a customer`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await customersPostCustomers(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
