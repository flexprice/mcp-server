/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { connectionsGetConnectionsId } from "../../funcs/connectionsGetConnectionsId.js";
import { GetConnectionsIdRequest$zodSchema } from "../../models/getconnectionsidop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetConnectionsIdRequest$zodSchema,
};

export const tool$connectionsGetConnectionsId: ToolDefinition<typeof args> = {
  name: "connections-get-connections-id",
  description: `Get a connection

Get a connection by ID`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await connectionsGetConnectionsId(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
