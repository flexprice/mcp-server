/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { scheduledTasksGetTasksScheduled } from "../../funcs/scheduledTasksGetTasksScheduled.js";
import { GetTasksScheduledRequest$zodSchema } from "../../models/gettasksscheduledop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetTasksScheduledRequest$zodSchema.optional(),
};

export const tool$scheduledTasksGetTasksScheduled: ToolDefinition<typeof args> =
  {
    name: "scheduled-tasks-get-tasks-scheduled",
    description: `List scheduled tasks

Get a list of scheduled tasks with optional filters`,
    annotations: {
      "title": "",
      "destructiveHint": false,
      "idempotentHint": false,
      "openWorldHint": false,
      "readOnlyHint": true,
    },
    args,
    tool: async (client, args, ctx) => {
      const [result] = await scheduledTasksGetTasksScheduled(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(result.value);
    },
  };
