/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { subscriptionsPostSubscriptionsIdPause } from "../../funcs/subscriptionsPostSubscriptionsIdPause.js";
import { PostSubscriptionsIdPauseRequest$zodSchema } from "../../models/postsubscriptionsidpauseop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: PostSubscriptionsIdPauseRequest$zodSchema,
};

export const tool$subscriptionsPostSubscriptionsIdPause: ToolDefinition<
  typeof args
> = {
  name: "subscriptions-post-subscriptions-id-pause",
  description: `Pause a subscription

Pause a subscription with the specified parameters`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await subscriptionsPostSubscriptionsIdPause(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
