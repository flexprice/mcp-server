/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { featuresPostFeaturesSearch } from "../../funcs/featuresPostFeaturesSearch.js";
import { TypesFeatureFilter$zodSchema } from "../../models/typesfeaturefilter.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: TypesFeatureFilter$zodSchema,
};

export const tool$featuresPostFeaturesSearch: ToolDefinition<typeof args> = {
  name: "features-post-features-search",
  description: `List features by filter

List features by filter`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await featuresPostFeaturesSearch(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
