/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { authPostAuthLogin } from "../../funcs/authPostAuthLogin.js";
import { DtoLoginRequest$zodSchema } from "../../models/dtologinrequest.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: DtoLoginRequest$zodSchema,
};

export const tool$authPostAuthLogin: ToolDefinition<typeof args> = {
  name: "auth-post-auth-login",
  description: `Login

Login a user`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await authPostAuthLogin(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
