/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { creditNotesPostCreditnotesIdVoid } from "../../funcs/creditNotesPostCreditnotesIdVoid.js";
import { PostCreditnotesIdVoidRequest$zodSchema } from "../../models/postcreditnotesidvoidop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: PostCreditnotesIdVoidRequest$zodSchema,
};

export const tool$creditNotesPostCreditnotesIdVoid: ToolDefinition<
  typeof args
> = {
  name: "credit-notes-post-creditnotes-id-void",
  description: `Void a credit note

Voids a credit note`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await creditNotesPostCreditnotesIdVoid(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
