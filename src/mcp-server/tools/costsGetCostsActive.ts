/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { costsGetCostsActive } from "../../funcs/costsGetCostsActive.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$costsGetCostsActive: ToolDefinition = {
  name: "costs-get-costs-active",
  description: `Get active costsheet for tenant

Get the active costsheet for the current tenant`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  tool: async (client, ctx) => {
    const [result] = await costsGetCostsActive(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
