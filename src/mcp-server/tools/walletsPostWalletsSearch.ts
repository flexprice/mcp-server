/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { walletsPostWalletsSearch } from "../../funcs/walletsPostWalletsSearch.js";
import { TypesWalletFilter$zodSchema } from "../../models/typeswalletfilter.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: TypesWalletFilter$zodSchema.optional(),
};

export const tool$walletsPostWalletsSearch: ToolDefinition<typeof args> = {
  name: "wallets-post-wallets-search",
  description: `List wallets by filter

List wallets by filter`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await walletsPostWalletsSearch(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
