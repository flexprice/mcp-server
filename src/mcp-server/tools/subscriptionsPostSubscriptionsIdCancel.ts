/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { subscriptionsPostSubscriptionsIdCancel } from "../../funcs/subscriptionsPostSubscriptionsIdCancel.js";
import { PostSubscriptionsIdCancelRequest$zodSchema } from "../../models/postsubscriptionsidcancelop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: PostSubscriptionsIdCancelRequest$zodSchema,
};

export const tool$subscriptionsPostSubscriptionsIdCancel: ToolDefinition<
  typeof args
> = {
  name: "subscriptions-post-subscriptions-id-cancel",
  description: `Cancel subscription

Cancel a subscription with enhanced proration support`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await subscriptionsPostSubscriptionsIdCancel(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
