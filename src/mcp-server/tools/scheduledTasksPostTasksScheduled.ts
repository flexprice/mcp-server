/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { scheduledTasksPostTasksScheduled } from "../../funcs/scheduledTasksPostTasksScheduled.js";
import { DtoCreateScheduledTaskRequest$zodSchema } from "../../models/dtocreatescheduledtaskrequest.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: DtoCreateScheduledTaskRequest$zodSchema,
};

export const tool$scheduledTasksPostTasksScheduled: ToolDefinition<
  typeof args
> = {
  name: "scheduled-tasks-post-tasks-scheduled",
  description: `Create a scheduled task

Create a new scheduled task for data export`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await scheduledTasksPostTasksScheduled(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
