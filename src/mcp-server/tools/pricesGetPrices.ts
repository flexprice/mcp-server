/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { pricesGetPrices } from "../../funcs/pricesGetPrices.js";
import { GetPricesRequest$zodSchema } from "../../models/getpricesop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: GetPricesRequest$zodSchema.optional(),
};

export const tool$pricesGetPrices: ToolDefinition<typeof args> = {
  name: "prices-get-prices",
  description: `Get prices

Get prices with the specified filter`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": true,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await pricesGetPrices(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
