/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { subscriptionsPostSubscriptionsIdChangePreview } from "../../funcs/subscriptionsPostSubscriptionsIdChangePreview.js";
import { PostSubscriptionsIdChangePreviewRequest$zodSchema } from "../../models/postsubscriptionsidchangepreviewop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: PostSubscriptionsIdChangePreviewRequest$zodSchema,
};

export const tool$subscriptionsPostSubscriptionsIdChangePreview: ToolDefinition<
  typeof args
> = {
  name: "subscriptions-post-subscriptions-id-change-preview",
  description: `Preview subscription plan change

Preview the impact of changing a subscription's plan, including proration calculations`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await subscriptionsPostSubscriptionsIdChangePreview(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
