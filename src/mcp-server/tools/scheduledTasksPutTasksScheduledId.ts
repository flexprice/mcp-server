/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { scheduledTasksPutTasksScheduledId } from "../../funcs/scheduledTasksPutTasksScheduledId.js";
import { PutTasksScheduledIdRequest$zodSchema } from "../../models/puttasksscheduledidop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: PutTasksScheduledIdRequest$zodSchema,
};

export const tool$scheduledTasksPutTasksScheduledId: ToolDefinition<
  typeof args
> = {
  name: "scheduled-tasks-put-tasks-scheduled-id",
  description: `Update a scheduled task

Update a scheduled task by ID - Only enabled field can be changed (pause/resume)`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await scheduledTasksPutTasksScheduledId(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
