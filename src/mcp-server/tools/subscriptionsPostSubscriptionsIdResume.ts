/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { subscriptionsPostSubscriptionsIdResume } from "../../funcs/subscriptionsPostSubscriptionsIdResume.js";
import { PostSubscriptionsIdResumeRequest$zodSchema } from "../../models/postsubscriptionsidresumeop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: PostSubscriptionsIdResumeRequest$zodSchema,
};

export const tool$subscriptionsPostSubscriptionsIdResume: ToolDefinition<
  typeof args
> = {
  name: "subscriptions-post-subscriptions-id-resume",
  description: `Resume a paused subscription

Resume a paused subscription with the specified parameters`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await subscriptionsPostSubscriptionsIdResume(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
