/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { invoicesPostInvoicesIdRecalculate } from "../../funcs/invoicesPostInvoicesIdRecalculate.js";
import { PostInvoicesIdRecalculateRequest$zodSchema } from "../../models/postinvoicesidrecalculateop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: PostInvoicesIdRecalculateRequest$zodSchema,
};

export const tool$invoicesPostInvoicesIdRecalculate: ToolDefinition<
  typeof args
> = {
  name: "invoices-post-invoices-id-recalculate",
  description: `Recalculate invoice totals and line items

Recalculate totals and line items for a draft invoice, useful when subscription line items or usage data has changed`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await invoicesPostInvoicesIdRecalculate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
