/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { creditGrantsPutCreditgrantsId } from "../../funcs/creditGrantsPutCreditgrantsId.js";
import { PutCreditgrantsIdRequest$zodSchema } from "../../models/putcreditgrantsidop.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: PutCreditgrantsIdRequest$zodSchema,
};

export const tool$creditGrantsPutCreditgrantsId: ToolDefinition<typeof args> = {
  name: "credit-grants-put-creditgrants-id",
  description: `Update a credit grant

Update a credit grant with the specified configuration`,
  annotations: {
    "title": "",
    "destructiveHint": false,
    "idempotentHint": false,
    "openWorldHint": false,
    "readOnlyHint": false,
  },
  args,
  tool: async (client, args, ctx) => {
    const [result] = await creditGrantsPutCreditgrantsId(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(result.value);
  },
};
